print("hello world from lox");

fun mul(a,b,c)
{
 var d= a*b*c;
 print d;
 return d;
}

var mul_result = mul(1,2,3);
print mul_result;
print mul;

var a = 1;
var b = 2;
var temp;

for (var b = 1; a < 10000; b = temp + b) {
  temp = a;
  a = b;
}

fun add(a, b) {
  print a + b;

  for (var b = 1; a < 10000; b = temp + b) {
  temp = a;
  a = b;

} 
}

fun sayHi(first, last) {
  print "Hi, " + first + " " + last + "!";
}

sayHi("Dear", "Reader");

print add; 
add(1,2);

